import { RENDERER_TYPES } from '../../types/rendererTypes'

const rendererTypes: RENDERER_TYPES = {
  LAYER: { render: 'Layer' },
  TEXT: { render: 'Text' },
  SHIP: { render: 'Ship' },
}

export default rendererTypes